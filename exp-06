#include<stdio.h>
int main()
{
    int n,sum=0,i=0;
    printf("Enter the range of number:");
    scanf("%d",&n);
    while(i<=n)
    {
        sum+=i;
        i+=2;
    }
    printf("The sum of the series = %d",sum);
}
